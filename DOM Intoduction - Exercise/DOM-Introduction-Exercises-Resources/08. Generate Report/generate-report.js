function solve() {
    // Select all checked table heads (th)
    const checkedColumns = document.querySelectorAll("th input[type=checkbox]:checked")
    console.log(checkedColumns)
    // Collect data from all checked columns in JSON for each row
}