function solve(arr) {
    const stringArr = arr.map(String);

    for (number of stringArr) {
        if (number === number.split('').reverse().join('')) {
            console.log('true')
        } else {
            console.log('false')
        }
    }
}

solve([123,323,421,121])
solve([32,2,232,1010])
